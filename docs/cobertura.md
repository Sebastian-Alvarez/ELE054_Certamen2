# Cobertura de Tests

Reporte generado por JaCoCo al ejecutar `./mvnw test`. Cubre controladores, servicios y repositorios.

> Para regenerar: `./mvnw test` — el reporte se actualiza en `target/site/jacoco/`.

<iframe src="jacoco/index.html" class="iframe-full"></iframe>
